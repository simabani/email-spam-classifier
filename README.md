# email-spam-classifier
Email spam classification with supervised learning

Train a supervised learning model to classify email messages as:
-  Spam (e.g. "You won $1000!")
-  Ham (non-spam, e.g. "Meeting at 3 PM")
Using natural language (text) as input.
